BACKEND_BASE_URL=http://localhost:1122
# Local benchmark only. Keep both values out of source control.
# BENCHMARK_CONTROL_TOKEN=backend-only-control-token
# BENCHMARK_OPERATOR_TOKEN=local-operator-password
# BENCHMARK_OPERATOR_ORIGIN=http://localhost:3000
